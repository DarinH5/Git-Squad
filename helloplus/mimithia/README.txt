Task Management
This is a simple Python command-line to-do list. Users can add tasks, view their current tasks, and mark tasks as completed.
To run the program, make sure Python is installed and run python hello_plus.py from the project directory.
This project relates to our group project because it demonstrates that we can handle basic user input from the user when we
eventually code the AI scrolling agent. 
